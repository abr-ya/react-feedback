import { StyledHeader, Container } from './styled/Model.styled';

function Header() {
  return (
    <Container>
      <StyledHeader>
        <h2>Feedback App</h2>
      </StyledHeader>
    </Container>
  );
}

export default Header;
